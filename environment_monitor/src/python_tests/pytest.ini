[pytest]
markers =
    hardware: requires a connected Arduino (skipped automatically if absent)
    manual: needs a person present to physically interact with a sensor

# Manual tests are excluded by default so the suite can run unattended.
# Run them explicitly with: pytest -m manual
addopts = -m "not manual" -v
